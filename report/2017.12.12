学习python：
1.Python 通常是一行写完一条语句，但如果语句很长，我们可以使用反斜杠(\)来实现多行语句
2.python中单引号和双引号使用完全相同，使用三引号('''或""")可以指定一个多行字符串。
3.自然字符串， 通过在字符串前加r或R。 如 r"this is a line with \n" 则\n会显示，而不是换行。
4.python允许处理unicode字符串，加前缀u或U， 如 u"this is an unicode string"。
5.字符串是不可变的。
6.函数之间或类的方法之间用空行分隔
中途遇到一个问题，即在cmd下可以import torch，但在idle下不行，目前未找到解决方法
